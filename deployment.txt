apiVersion: apps/v1 
kind: Deployment
metadata:
  name: predict-imdb 
  labels: 
    app: myke
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: myke
  template:
    metadata:
      labels:
        app: myke
    spec:
      containers:
      - name: myke
        image: pytorch/pytorch
        command: ["/bin/sh", "-c"]
        args:
         - git clone https://github.com/yogeshbendre/predictme;
           cd predictme;
           pip3 install torch;
		   pip3 install flask;
           python3 runflask.py;
        ports:
        - containerPort: 8080
 